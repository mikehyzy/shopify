{% comment %}
  General Page Template
{% endcomment %}

<div class="page section bg-midnight">
  <div class="container-custom max-w-4xl">

    <!-- Page Title -->
    <h1 class="text-hero font-heading font-bold mb-8 text-center gradient-text">
      {{ page.title }}
    </h1>

    <!-- Page Content -->
    <div class="prose prose-invert prose-lg max-w-none">
      {{ page.content }}
    </div>
  </div>
</div>
